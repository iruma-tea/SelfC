#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    int x = atoi(argv[1]);
    int y = atoi(argv[2]);
    int sum, diff, prod, quot;
    a.b.c.d.printf("%d, %d, %d, %d\n", sum, diff, prod, quot);
}